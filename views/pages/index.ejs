<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

  <% include ../partials/nav.ejs %>

<div class="jumbotron text-center">
  <div class="container">
    <a href="/" class="lang-logo">
      <img src="/cm2_logo.png">
    </a>
    <h1>Code Marathon 2.0</h1>
    <p>Официальная страница контеста проводимого для учеников ОСШИ "Одаренность"</p>
  </div>
</div>
<div class="container">
  <div class="alert alert-info text-center" role="alert">
    <h2>Таблица результатов</h2>
    <p>Обновление происходит с задержкой в 10-15 минут.</p>
  </div>
  <hr>
  <div class="row">
      <table class="table">
        <caption><h3>Div1</h3></caption>
      <thead>
        <tr>
          <th>#</th>
          <th>ФИО</th>
          <th>ACMP ID</th>
          <th>Дата и время регистрации</th>
          <th>Стартовый рейтинг</th>
          <th>Текущий рейтинг</th>
          <th>Бонусы</th>
          <th>Итоговый рейтинг</th>
        </tr>
      </thead>
      <tbody>
        <% div1Students.forEach(function(student, index) { %>
                    <tr<% if(index < 6){ %> class="bg-success" <% } %>> 
                        <th scope="row"><%= index+1 %></th>
                        <td><%= student.fullname %></td> 
                        <td><%= student.acmpId %></td> 
                        <td><%= student.dateTime %></td>
                        <td><%= student.startRating %></td>
                        <td><%= student.currentRating %></td>
                        <td><%= student.bonusRating %></td>
                        <td><%= student.contestRating %></td>
                    </tr> 
                <% }); %>
      </tbody>
    </table>
  </div> <!-- row -->
  <div class="row">
      <table class="table">
        <caption><h3>Div2</h3></caption>
      <thead>
        <tr>
          <th>#</th>
          <th>ФИО</th>
          <th>ACMP ID</th>
          <th>Дата и время регистрации</th>
          <th>Стартовый рейтинг</th>
          <th>Текущий рейтинг</th>
          <th>Бонусы</th>
          <th>Итоговый рейтинг</th>
        </tr>
      </thead>
      <tbody>
        <% div2Students.forEach(function(student, index) { %>
                    <tr<% if(index < 6){ %> class="bg-success" <% } %>>  
                        <th scope="row"><%= index+1 %></th>
                        <td><%= student.fullname %></td> 
                        <td><%= student.acmpId %></td> 
                        <td><%= student.dateTime %></td>
                        <td><%= student.startRating %></td>
                        <td><%= student.currentRating %></td>
                        <td><%= student.bonusRating %></td>
                        <td><%= student.contestRating %></td>
                    </tr> 
                <% }); %>
      </tbody>
    </table>
  </div> <!-- row -->
  <div class="row">
      <table class="table">
        <caption><h3>Div3</h3></caption>
      <thead>
        <tr>
          <th>#</th>
          <th>ФИО</th>
          <th>ACMP ID</th>
          <th>Дата и время регистрации</th>
          <th>Стартовый рейтинг</th>
          <th>Текущий рейтинг</th>
          <th>Бонусы</th>
          <th>Итоговый рейтинг</th>
        </tr>
      </thead>
      <tbody>
        <% div3Students.forEach(function(student, index) { %>
                    <tr<% if(index < 6){ %> class="bg-success" <% } %>> 
                        <th scope="row"><%= index+1 %></th>
                        <td><%= student.fullname %></td> 
                        <td><%= student.acmpId %></td> 
                        <td><%= student.dateTime %></td>
                        <td><%= student.startRating %></td>
                        <td><%= student.currentRating %></td>
                        <td><%= student.bonusRating %></td>
                        <td><%= student.contestRating %></td>
                    </tr> 
                <% }); %>
      </tbody>
    </table>
  </div> <!-- row -->
   <div class="alert alert-info text-center" role="alert">
    Да прибудет с Вами сила...
  </div>
</div>


</body>
</html>
